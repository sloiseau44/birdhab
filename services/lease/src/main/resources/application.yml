spring:
  application:
    name: lease-service

  datasource:
    url: jdbc:postgresql://localhost:5432/birdhab
    username: birdhab
    password: birdhab
    hikari:
      # Neon suspend son instance après quelques minutes d'inactivité et ferme les
      # connexions existantes — voir le commentaire détaillé dans auth/application.yml.
      max-lifetime: 180000       # 3 min, sous le délai de suspension Neon
      maximum-pool-size: 5       # usage faible volume (self-hosted)

  jpa:
    hibernate:
      ddl-auto: validate   # le schéma est piloté par Flyway, jamais par Hibernate
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
        default_schema: lease

  flyway:
    enabled: true
    locations: classpath:db/migration
    schemas: lease   # schéma Postgres dédié : la base "birdhab" est partagée entre
                     # microservices, mais chaque service isole ses tables et son
                     # historique flyway_schema_history dans son propre schéma
                     # (sinon collision avec celui du service auth, en schéma public)
    default-schema: lease

server:
  port: 8084

# Config JWT — même secret que le service auth (birdhab.jwt.secret) : lease ne
# génère jamais de jetons, il valide uniquement ceux émis par auth.
birdhab:
  jwt:
    secret: ${JWT_SECRET:changeme-local-dev-secret-key-not-for-production-use-only}

springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /v3/api-docs
